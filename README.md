# 📌Task-Manager

Una piccola applicazione in **Python** per **gestire le attività (task)** in modo semplice e modulare.

## Caratteristiche principali

- Creazione, visualizzazione, modifica e cancellazione di task.  
- Architettura modulare con separazione tra logica applicativa (nel pacchetto `src/task_manager`) e test (in `tests/`).  
- Configurato con `pyproject.toml`, anche per facilitare packaging/installazione.  
- Licenza: **GPL-3.0**. (come indicato nel file LICENSE)

## Tecnologie e requisiti

- Python 3.13  
- Dipendenze elencate in `pyproject.toml`  


## Installazione

 ```bash
 git clone https://github.com/Omisen/Task-Manager.git
 cd Task-Manager
 poetry install
```


